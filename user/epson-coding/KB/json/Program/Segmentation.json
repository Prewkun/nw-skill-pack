{
  "title": "Segmentation",
  "file": "Segmentation",
  "section": "Program",
  "type": "reference",
  "syntax": "",
  "parameters": "",
  "description": "Segmentation\n\nSegmentation\n\nIn order to make measurements of an object's features, blob analysis must first determine where in the image an object is: that is, it must separate the object from everything else in the image.  The process of splitting an image into objects and background is called segmentation\n\n.\n\nThe Blob object is intended for use on an image that can be segmented based on the grey-scale value of each of its pixels.\n  A simple example of this kind of segmentation is thresholding.\n\nWhile the Blob object described in this section produces well defined results even on arbitrary grey scale images that cannot be segmented by grey-scale value, such cases are typically of limited utility because the results are influenced by the size of the window defining the image.\n\nFor whole image blob analysis, the feature of interest must be the only object in the image having a particular grey level.  If another object in the image has the same grey-scale value, the image cannot be segmented successfully.  The following figures illustrate which can and cannot be segmented by grey-scale value for whole image blob analysis.\n\nScene that can be segmented by grey-scale value.\n\nThe figure above shows the camera's field of view (left). The scene to be processed using a Blob object falls within the search window\n\nlabeled \"Blob01\".  After segmentation by grey-scale value, the object and the background are easily distinguishable as shown on the right side of the figure.\n\nScene from the previous figure with larger search window\n\nChanging the size of the search window as shown above, changes only the size of the background.  It has no effect on the features of the blob.\n\nThe figures below show a similar field of view for an image.  However, this scene cannot be segmented by grey-scale value because there are two objects in the image which have the same grey-scale value.  These objects can be separated only by eliminating one or the other from within the search window.  While the image in the first figure could be segmented by grey-scale value, enlarging the search window as shown in the second figure, completely changes the segmented image.  For this scene, both the area of the background and the measured features of the blob vary depending on the size of the search window and the portion of the image it encloses.\n\nScene that cannot be segmented by grey-scale value\n\nEnlarged search window encloses two objects with the same grey scale value\n\nYou should also watch out for situations as shown below. In this example, the inner blob and part of the background have become connected.  This forms one large blob which has very different features from the original center blob which we are trying to segment.\n\nObject and background have been connected",
  "notes": "",
  "see_also": "",
  "examples": []
}